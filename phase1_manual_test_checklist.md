# MONO Phase 1 수동 검수 체크리스트

아래 순서대로 테스트하면 Phase 1(인증/연락처/홈/설정) 핵심 흐름을 빠르게 검증할 수 있습니다.

## 자동 검수 결과 업데이트 (2026-02-24)

- 실행 환경: 로컬 서버(`127.0.0.1:3174`) + SQLite(`state/mono_phase1.sqlite`)
- 자동 검수 요약:
  - API/DB/빌드: 통과
  - 브라우저 실기기 항목: 수동 검수 필요
- 주요 통과 항목:
  - `/healthz` 정상 응답
  - `users/friends/rooms/room_members` 테이블 존재 확인
  - 비로그인 인증 차단(`401`) 확인
  - 친구 검색/요청/수락/목록 API 확인
  - 프로필 수정/로그아웃 API 확인
  - `npm run build` 성공

## 0) 사전 준비

- 서버 실행
  - `npm start`
- 클라이언트 빌드/서빙 상태 확인
  - `npm run build`
- 브라우저 2개 계정 준비(또는 시크릿 창 2개)
  - 계정 A(호스트), 계정 B(상대)

## 1) 루트 진입 분기

- [ ] 비로그인 상태에서 `/` 접속 시 `통역 탭(/interpret)`으로 이동 (브라우저 수동 확인 필요)
- [ ] 로그인 상태(쿠키 유효)에서 `/` 접속 시 `홈 탭(/home)`으로 이동 (브라우저 수동 확인 필요)
- [ ] `/?roomId=...` 접속 시 `interpret?roomId=...`로 전달되어 기존 QR 흐름 유지 (브라우저 수동 확인 필요)

## 2) 인증 API

- [x] Google 로그인 완료 후 `GET /api/auth/me`가 `authenticated: true` 반환 (JWT 기반 API 검증 완료)
- [x] 로그인 직후 `users` 테이블에 사용자 row 생성/업데이트 확인 (QA 계정 upsert 확인)
- [ ] `POST /api/auth/logout` 후 새로고침 시 비로그인 분기 동작 (브라우저 수동 확인 필요)

## 3) 설정 탭

- [x] 닉네임 수정 후 저장 시 성공 메시지 확인 (API 수정 반영 확인)
- [ ] MONO ID 중복 입력 시 에러 메시지 확인 (수동 확인 필요)
- [x] 모국어 변경 후 저장 시 재진입 시 값 유지 확인 (API 재조회 확인)
- [ ] 로그아웃 버튼 클릭 시 `interpret`로 이동 + 로컬 identity 정리 확인 (브라우저 수동 확인 필요)

## 4) 연락처 탭 (친구 기능)

- [x] MONO ID 검색(2글자 이상) 결과 노출 (API 검색 검증 완료)
- [x] 친구 요청 보내기 동작 (API 검증 완료)
- [x] 상대 계정에서 요청 수락/거절/차단 동작 (수락 API 검증 완료, 거절/차단은 수동 추가 확인 권장)
- [x] 수락 후 내 친구 목록에 상대 표시 (양쪽 friends 조회 확인)
- [ ] 친구 삭제 동작(양방향 관계 제거) (수동 확인 필요)

## 5) 새 대화 생성 (홈 탭)

- [ ] 홈 탭의 `+ 새 대화` 클릭 시 친구 목록 기반 모달 표시 (브라우저 수동 확인 필요)
- [ ] 친구 선택 시 `create-1to1`로 1:1 방 생성 (브라우저 수동 확인 필요)
- [ ] 생성된 방으로 자동 진입 (브라우저 수동 확인 필요)

## 6) 대화 목록(홈) 메타 반영

- [ ] 메시지 송신 후 목록에 마지막 메시지/시간 반영 (브라우저 수동 확인 필요)
- [ ] 상대 메시지 수신 시 unread 뱃지 증가 (브라우저 수동 확인 필요)
- [ ] 방 입장 후 unread 초기화 (브라우저 수동 확인 필요)
- [ ] 당일 시간은 시:분, 이전 날짜는 날짜로 표시 (브라우저 수동 확인 필요)

## 7) 기존 통역 회귀 (필수)

- [ ] QR 방 생성/입장 정상 (브라우저/실기기 수동 확인 필요)
- [ ] 텍스트 송수신 정상 (브라우저/실기기 수동 확인 필요)
- [ ] 마이크 STT 송수신 정상 (브라우저/실기기 수동 확인 필요)
- [ ] 번역 결과 표시 정상 (브라우저/실기기 수동 확인 필요)

## 8) 실패 시 우선 점검 포인트

- 인증 실패: `JWT_SECRET`, OAuth callback URL, 쿠키 도메인/https
- 친구 검색 실패: `/api/contacts/*` 응답 코드, 로그인 쿠키 포함 여부
- 대화 생성 실패: 소켓 연결 상태, `create-1to1` payload의 `myUserId/peerUserId`
- 목록 미반영: IndexedDB(`rooms`) 업데이트 여부, `recordRoomActivity` 호출 여부

