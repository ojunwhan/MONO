# Room UI Update Log

## 2026-02-25

- 입력 영역 레이아웃 정리
  - 중앙의 큰 마이크 버튼 제거
  - 하단 입력 구조를 `[+첨부] [텍스트 입력창] [오른쪽 액션]`으로 통일
  - 텍스트 비어있을 때 오른쪽 액션을 `MicButton` compact 모드로 연결
  - 텍스트 입력 시 오른쪽 액션을 `SendHorizontal` 전송 버튼으로 전환
- 상단 `⋮` 메뉴 동작 연결
  - 클릭 시 우측 드롭다운 오픈
  - 메뉴: 알림 설정, 대화방 고정, 대화 내 검색, 대화방 나가기
  - 메뉴 바깥 클릭 시 자동 닫힘
- 메시지 버블 분기 강화
  - `senderId === currentUserId` 조건으로 내 메시지/상대 메시지 분기 보강
  - 내 메시지: 우측 정렬, 파란 배경(`#3B82F6`), 흰색 텍스트, `16px 16px 4px 16px`
  - 상대 메시지: 좌측 정렬, 회색 배경(`#F0F0F0`), 진한 텍스트, `16px 16px 16px 4px`
  - 그룹방에서만 상대 아바타(36px)/닉네임 라벨 표시
  - 날짜 구분선(가운데 + 양쪽 라인) 추가
  - 연속 메시지 간격 규칙 적용(같은 발신자 4px, 다른 발신자 16px)
- CS 챗봇 백엔드 STEP 1
  - `POST /api/cs-chat` 엔드포인트 추가 (`server/routes/auth_api.js`)
  - 요청 본문: `message`, `language`, `history`(최대 10개)
  - 응답 본문: `reply`
  - GPT-4o 계열 모델 호출(`CS_CHAT_MODEL` 또는 기본 `gpt-4o-mini`)
  - 번역 경로와 분리된 CS 전용 system prompt 적용
  - 사용자가 제공한 모노봇 시스템 프롬프트 전문으로 교체
  - `history`를 대화 맥락용 `messages` 배열로 전달 유지
  - `temperature: 0.3`, `max_tokens: 500` 적용
- CS 챗봇 프론트엔드 STEP 2
  - 설정 화면에 고객센터 섹션 추가
    - `💬 MONO 도우미` → `/cs-chat` 이동
    - `📧 이메일 문의` → `mailto:support@lingora.chat`
  - `/cs-chat` 페이지 신설
    - 상단 헤더(뒤로가기 + MONO 도우미 + AI 고객지원)
    - 기존 `MessageBubble` 재활용(내 메시지/봇 메시지 버블)
    - 초기 인사 메시지 + 빠른 질문 칩 4개
    - 텍스트 전용 입력창(첨부/마이크 숨김)
    - 전송 시 로딩 점 애니메이션 후 `/api/cs-chat` 응답 반영
    - `history` user/assistant 쌍 최대 10개 유지
    - 페이지 이탈 시 히스토리 초기화(localStorage 미사용)
  - 라우팅 추가: `/cs-chat` (하단 탭 숨김)